# Build Progress

One line per completed phase. See `PHASE_N_TASKS.md` files for per-phase task detail and `docs/PROJECT.md` for the spec.

## Phase 0 — Project bootstrap — 2026-05-03
Postgres `goan_perfume` connected; `is_admin` column + `EnsureUserIsAdmin` middleware (alias `admin`) protect `/dashboard`; admin seeded as `admin@goanperfume.de`/`password` (idempotent `AdminUserSeeder`); `storage:link` done; `APP_NAME="Goan Perfume"`; `README.md` written. **Side note**: enabled `RefreshDatabase` in `tests/Pest.php` (starter kit shipped with it commented, breaking all Feature tests). Final verification: `migrate:fresh --seed` clean, 41/41 Pest tests pass, Pint clean.

## Phase 1 — Schema & seed data — 2026-05-03
12 migrations cover sections 4.1–4.9 of `docs/PROJECT.md`; sequential timestamps so FK order is correct. `timestampsTz()` everywhere relevant; `jsonb` on `page_sections.payload`; pivots use composite PKs with `cascadeOnDelete`; `categories.parent_id` uses `nullOnDelete`. Custom polymorphic `translations` table (no spatie/astrotomic). Minimal `Product` + `ProductVariant` models with `#[Fillable]` only — relationships and translation/slug traits stay for Phase 2. Three new idempotent seeders (`CategorySeeder`, `AttributeSeeder`, `AttributeValueSeeder`) write entities and their German `translations` rows; slugs via `Str::slug($name, '-', 'de')` for proper umlaut transliteration. Verified: 7 categories, 4 attributes, 63 attribute values, 74 German translation rows; Postgres columns confirmed `jsonb` + `timestamptz`; 41/41 tests pass; Pint clean. **Carry-over**: translation rows reference `App\Models\Category` / `App\Models\Attribute` / `App\Models\AttributeValue` as literal strings — if Phase 2 introduces a morph map, the seeders and any persisted rows need to be aligned.

## Scope change — 2026-05-03 — drop SKUs and stock counts
Showcase site, not e-commerce → no inventory/SKU tracking on the website. Removed `products.sku_base`, `product_variants.sku` (incl. its UNIQUE constraint), and `product_variants.stock_quantity`. Updated: `docs/PROJECT.md` §4 design-decision blurb and §4.3 schema; both `create_products_table` and `create_product_variants_table` migrations; `Product` and `ProductVariant` models (`#[Fillable]` + casts); `ProductFactory` and `ProductVariantFactory` (incl. `size()` state). `compare_at_price` kept (display-only "was/now" marketing). Verification: `migrate:fresh --seed` clean, 58/58 tests still pass, Pint clean. Postgres columns confirmed dropped via `\d products` / `\d product_variants`.

## Phase 2 — Models & business logic — 2026-05-03
8 new models filled in (`Category`, `Attribute`, `AttributeValue`, `Media`, `Translation`, `PageSection`, `Promotion`, `Setting`); `Product` and `ProductVariant` extended with relationships per the section-5 ERD. Two new traits in `app/Models/Concerns/`: `HasTranslations` (`translations()` morphMany, `translate(locale, field, fallbackLocale)`, `setTranslation(...)` that refreshes a loaded relation) and `HasSlug` (transient `setSlugSource()` setter, `Str::slug(..., '-', 'de')`, collision suffix `-2/-3`, excludes self on update). `Promotion::scopeActive()` matches the section-4.8 query verbatim. `Setting` is a key-PK string model with `CREATED_AT = null` and static `get()`/`put()` helpers. Phase 1 seeders refactored to construct entities via models and call `setTranslation('de','name', ...)` — exercises both new traits during `migrate:fresh --seed`. **Side adjustments**: pinned `'product_attribute_value'` pivot name on both ends of `Product` ↔ `AttributeValue` (Eloquent's default `attribute_value_product` didn't match the spec); moved the new trait tests into `tests/Feature/` because `RefreshDatabase` is only wired to that suite. Decision confirmed: no morph map — default Eloquent behavior matches the literal FQN strings already in `translations.translatable_type`. Final verification: 58/58 Pest tests pass (161 assertions); `migrate:fresh --seed` clean; Pint clean; tinker round-trip on Phase-1 data through Phase-2 models works.

## Phase 3 — Admin dashboard, Categories + Attributes slice — 2026-05-08
Dashboard resource routing moved into `routes/dashboard.php` behind `auth`, `verified`, and `admin`. Category CRUD is implemented with German-only admin labels, DE/AR/EN translation tabs, slug override, parent selection with server-side cycle rejection, banner upload/removal, and image/translation cleanup tests. Attribute CRUD is implemented with nested AttributeValue management on the edit screen: scoped value uniqueness, scoped generated slugs, nested ownership enforcement, and translation cleanup. Shared dashboard components landed for translation tabs, slug fields, media upload, and data tables; sidebar now includes Kategorien and Attribute; Wayfinder is generated by the Vite plugin during build. Verification for the two landed resources: `php artisan test --compact tests/Feature/Dashboard/AttributeControllerTest.php tests/Feature/Dashboard/CategoryControllerTest.php` passes with 22 tests / 142 assertions; `npm run build` passes; Pint clean. Carry-over: Products, Promotions, Page Sections, Settings, and the reusable backend `MediaService` remain in Phase 3.

## Phase 3 — Media service — 2026-05-08
Added `App\Services\MediaService` for reusable polymorphic media syncing. It stores uploaded files on the configured disk under `media/{model-type}/{model-id}/{ulid}.{ext}`, updates existing media metadata, deletes removed rows plus files and translations, syncs DE/AR/EN `alt_text` translations, preserves omitted existing rows, and guarantees one primary media item when media remains. Added `tests/Feature/MediaServiceTest.php` with `Storage::fake('public')` covering create, update, delete, primary enforcement, translation sync, and unsaved-model rejection. Carry-over: Product CRUD will be the first controller to consume the service.

## Phase 3 — Product form prerequisite — 2026-05-08
Added `resources/js/components/dashboard/variant-row-editor.tsx`, a controlled React component for product variant rows. It supports adding/removing variants, editing size/price/compare-at price, active/default toggles, one-default enforcement in the UI, and Laravel dotted validation errors such as `variants.0.price`. This unblocks the Product create/edit form.

## Phase 3 — Products slice — 2026-05-08
Product CRUD is implemented under `/dashboard/products`. The index shows primary image, German name, brand, categories, price range from variants, active status, and featured status. Create/edit uses one multipart Inertia form for translations, slug/brand/status, category multi-select, grouped attribute values with single-select enforcement, variants, and multi-image media via `MediaService`. The controller saves the full product graph in a transaction and cleans up media files/translations on delete. Sidebar now includes Produkte. Verification so far: `php artisan test --compact tests/Feature/Dashboard/CategoryControllerTest.php tests/Feature/Dashboard/AttributeControllerTest.php tests/Feature/Dashboard/ProductControllerTest.php` passes with 28 tests / 205 assertions; `npm run build` passes.

## Phase 3 — Promotions slice — 2026-05-08
Promotion CRUD is implemented under `/dashboard/promotions`. The index shows title, image, discount, schedule window, sort, and active/upcoming/expired/inactive status. Create/edit supports DE/AR/EN hero copy (`badge`, `title`, `subtitle`, `cta_text`), slug override, background image upload/removal, fallback background color, link URL, promo code, discount percent, schedule window, sort, and active flag. Sidebar now includes Aktionen. Feature tests cover access control, index status payloads, create with background image and active-scope round-trip, validation, update with image replacement and translation cleanup, image removal, and delete cleanup.

## Phase 3 — Page Sections slice — 2026-05-08
Page Sections are implemented as an edit-only fixed resource under `/dashboard/page-sections`. `PageSectionSeeder` now idempotently creates `hero`, `about`, `why_us`, and `featured_products` during normal seeding. The index shows the four sections with status and summary, and the edit screen switches between the matching payload editors: hero image + CTA copy, about title/body, translatable why-us bullet lists, and ordered featured product IDs. Sidebar now includes Seiten-Inhalt. Feature tests cover access control, idempotent seeding, index payloads, hero image replacement, about copy, bullet-list round-trip, ordered product IDs, and invalid product IDs.

## Phase 3 — Settings slice — 2026-05-08
Site settings are implemented under `/dashboard/settings/site`, separate from the user-account `/settings/*` pages. The form manages WhatsApp, email, phone, Instagram/TikTok/Facebook URLs, default locale, and logo upload/removal through the existing `settings` key-value table. Logo files are stored under `storage/app/public/branding/`, and the sidebar now includes Einstellungen. Feature tests cover access control, loading current values, full key round-trip with logo upload, logo replacement/removal cleanup, and validation for email, URLs, and locale.

## Phase 3 — Admin dashboard complete — 2026-05-08
Phase 3 admin dashboard scope is complete. Dashboard resources now cover Categories, Attributes + inline Attribute Values, Products + variants/media, Promotions, fixed Page Sections, and Site Settings. The sidebar contains all German admin entries with a separator before Einstellungen, dashboard page titles use the shared German admin title helper, and `PHASE_3_TASKS.md` is fully checked off. Final verification for this completion pass: `php artisan migrate:fresh --seed`, `vendor/bin/pint --dirty --format agent`, and `composer ci:check`.

## Phase 4 — Public frontend complete — 2026-05-15
German public pages are implemented with controller-backed Inertia routes for home, category, product detail, pricing, and contact. Public props are mapped explicitly from real categories, products, variants, media, promotions, page sections, and settings; category filters preserve OR-within-group and AND-across-groups behavior; image URLs use stable storage paths. `PHASE_4_TASKS.md` is fully checked off. Verification: `php artisan route:list --path=dashboard`, `php artisan route:list --except-vendor`, `php artisan test --compact tests/Feature/PublicPagesTest.php tests/Feature/FilterQueryTest.php`, `npm run build`, `vendor/bin/pint --dirty --format agent`, and `composer ci:check`.
